import Button from "@mui/material/Button";
import Avatar from "@mui/material/Avatar";

const ProfileAvatar = () => {
  return (
    <Button color="inherit">
      <Avatar sx={{ backgroundColor: "purple" }}>FL</Avatar>
    </Button>
  );
};

export default ProfileAvatar;
